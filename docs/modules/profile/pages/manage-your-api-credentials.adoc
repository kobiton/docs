= Manage your API credentials
:navtitle: Manage your API credentials

Manage the API base URL, Appium server URL, and API keys to use in your xref:automation-testing:scripting/auto-generate-an-appium-script.adoc[Appium scripts,window=read-later], xref:integrations:index.adoc[integration configuration files,window=read-later], and link:https://api.kobiton.com/v2/docs[API requests,window=read-later].

== Retrieve API base URL

Use the API base URL to make requests to the Kobiton API. To retrieve the URL:

include::help-widget:partial$get-api-base-url.adoc[]

== View your credentials

include::profile:partial$open-settings.adoc[]

Select *API Keys*.

image:profile:api-key-context.png[width=1000,alt="Select API Keys on the top nav bar"]

Each key in the list shows its name, value, creation date, and available actions.

== Manage your credentials

[#_get_server_url]
=== Get the Appium server URL

The Appium server URL is used for Appium automation testing. Select the *Copy* icon to add it to your clipboard.

// TODO KOB-54604: this screenshot predates the API Keys page redesign. Refresh after the redesign reaches production.
image:profile:server-url-closeup.png[width=500,alt="A Server URL section in API Keys"]

[#_get_an_api_key]
=== Get an API key

A default API key is automatically generated when you create an account. Select the *Copy* icon to add a key to your clipboard. Copying works whether the key is hidden or shown.

// TODO KOB-54604: this screenshot predates the API Keys page redesign and shows a key in plain text. Refresh after the redesign reaches production.
image:profile:copy-api-closeup.png[width=500,alt="Click the Copy icon to copy API"]

[#_show_or_hide_an_api_key]
=== Show or hide an API key

API keys are hidden each time you open the API Keys page. Select the eye icon next to a key to show its value, then select the icon again to hide it.

Each key has its own control, so showing one key leaves the rest hidden. A key stays visible until you hide it or leave the page. Every key returns to the hidden state when you open the page again.

=== Create a new API key

To create a new API key, select *Generate New API Key*.

// TODO KOB-54604: this screenshot predates the API Keys page redesign and shows the former *Add key* label. Refresh after the redesign reaches production.
image:profile:add-key-closeup.png[width=500,alt="A closeup to select Add key in API keys"]

=== Rename an API key

To rename an API key, select the key's current name, then enter a new one.

// TODO KOB-54604: this screenshot predates the API Keys page redesign. Refresh after the redesign reaches production.
image:profile:renamed-api-closeup.png[width=500,alt=""]
