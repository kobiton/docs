= Metadata for the device utilization report
:navtitle: Report metadata

The device utilization report is a predefined report measuring device usage by sessions, testing minutes, and unique users for a selected date range. For more information, see xref:reporting:device-utilization-report/use-the-report.adoc[].

// Screenshots pending. Add report context images before publishing.

== Data included

The report covers device usage recorded within the selected date range. Sessions run by users with a `kobiton.com` email address are excluded, so an organization whose only activity comes from those accounts shows no data.

Rows are sorted by *Sessions* in descending order.

== Report fields

=== Device Name

The name of the device.

=== Custom Device Name

The custom name assigned to the device by your organization.

=== Manufacturer

The device manufacturer.

=== OS

The device operating system.

=== Version

The device operating system version.

=== UDID

The unique device identifier.

=== Team

The team the recorded usage belongs to.

=== Public/Private

Whether the device is a public device or a private device.

=== Sessions

The number of test sessions run on the device within the selected date range.

=== Minutes

The number of testing minutes recorded on the device within the selected date range.

=== Unique Users

The number of distinct users who ran a session on the device within the selected date range.
