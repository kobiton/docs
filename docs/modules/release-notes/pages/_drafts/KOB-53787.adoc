////
DRAFT — NOT READY FOR PUBLICATION
Generated by the Robot Ranch docs-workflow (Ledger / docs-writer skill).
Ticket: KOB-53787
Mode: release-note
Status: BLOCKED — no source evidence available at generation time.

This file is intentionally NOT wired into release-notes/nav.adoc and is
parked under pages/_drafts/ so it does not enter published navigation.
It exists so the docs team can review the open questions below in the PR.
Do not merge into a release until the open questions are resolved and the
draft is rewritten from approved evidence.
////
= Release note draft: KOB-53787 (BLOCKED — no source evidence)
:navtitle: KOB-53787 (draft)

[WARNING]
====
This is an unverified draft stub, not a release note. Ledger (the docs-writer
skill) could not locate any source evidence for KOB-53787, so no user-facing
change has been drafted. Nothing below describes confirmed product behavior.
Resolve the open questions, then regenerate from approved evidence.
====

== Mode

`release-note`

== Source evidence used

None. No usable source material was available when this draft was generated:

* No `features/KOB-53787*` spec folder exists on the `.ai` `main` branch (the
  ref this run was given).
* No Jira or Atlassian integration was reachable in this environment, so the
  Dish (`/docs-workflow:jira-draft`) step could not fetch or normalize the
  ticket.
* No PR summary, code-diff summary, or human-supplied description was provided.

Per the docs-writer core rule "Do not proceed without source evidence," no
release-note body was drafted. Inventing the change would violate the skill's
guardrails against fabricating product details, UI labels, and behavior.

== Draft output

_No draft produced._ A release note requires a confirmed, user-visible change.
The substance of KOB-53787 is unknown pending the evidence requested below.

Once evidence is available, this is the expected shape (bug fix vs. feature
determines which structure applies):

* *Bug fix or smaller improvement* — a short grouped item under the relevant
  area in the current release page (for example, "Fixed an issue where ...").
* *New feature or notable enhancement* — title, short summary, optional
  highlights, and availability, added to the in-progress release page and wired
  into `release-notes/nav.adoc`.

== User-facing impact summary

Unknown. Cannot be determined without source evidence. Do not assume the change
has shipped, is active, or is user-visible until the ticket confirms it.

== Open questions or verification needed

. *Provide source evidence for KOB-53787.* Attach the Jira ticket (summary,
  description, status, acceptance criteria), a PR summary, or an approved change
  description. This is the blocking item — nothing can be drafted without it.
. *Confirm this ticket is release-note-worthy.* Run Valentine
  (`/docs-workflow:jira-bucketer`) against the ticket to confirm the
  `Release note only` / `Yes - major|minor` bucket. It may instead be
  `No doc impact`, `Needs SME`, or `Docs needed` (a full guide rather than a
  release note).
. *Confirm the change type* — bug fix vs. new feature vs. behavior change — so
  the correct release-note shape and section are used.
. *Confirm the target release version and date.* The newest published release is
  4.26 (May 9, 2026); this draft does not assume a version number.
. *Confirm the affected product surface and exact UI labels* before any control,
  setting, or API name is written. Do not infer API endpoints, reverse actions,
  or recovery paths from UI terminology.
. *Confirm availability conditions* (subscription, entitlement, beta, deployment
  type) if the change is gated.
. *Decide whether a companion doc is needed.* If the change requires setup or
  conceptual grounding, create or update a guide in addition to the release note.
