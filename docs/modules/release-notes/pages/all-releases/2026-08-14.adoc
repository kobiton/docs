= Cloud August 14, 2026

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by the build date in YYYYMM.DD format. For example, 202608.14 represents a build created on August 14, 2026.

Cloud release notes are typically published weekly and summarize changes since the previous publication. +
Version information for individual software components is available in the Component versions section.
====

// Generated by The Gazette (catch-up compilation).
// Review structure, release date, and entry placement before publishing.
// Dropped from this bucket during compilation (fixVersion outside Aug 10-14 window):
//   KOB-52016 (Q2 - 4.26), KOB-52092 (2026.07.07), KOB-52245 (2026.06.11),
//   KOB-53297 (2026.06.29 + MCP tool minor), KOB-53592 (2026.07.03/06),
//   KOB-53653 (2026.07.07 + internal API), KOB-53791 (2026.07.02),
//   KOB-52406 (Q2 - 4.26).
// Also dropped as internal-only: KOB-53672 (R2 auto-upgrade infra plumbing, no user-visible change).
// Dropped during refinement:
//   KOB-52121 (Appium find element with LLM vision / visionvirtual locator) — already documented
//   in 4.26S as "Appium AI: Natural language locators (Beta)" (2026-04-something ship). This is
//   the same feature; the updated_fallback bucket landed it here on the Aug 11 update timestamp,
//   but the feature was released long before this window.

== Kobiton version management

- Force-offline enforcement based on deviceConnect version age has been removed. Machines are no longer taken offline for being one or more major versions behind, and the upgrade-deadline concept has been retired.
- The orange warning banner for outdated deviceConnect versions has been removed. A purple notification banner will continue to appear when a newer deviceConnect version is available. Admins can still initiate a manual upgrade from Device Management, and the automatic/manual upgrade toggle is unchanged.

// REVIEWER NOTE:
// KOB-48664 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-10 | bucket_source: fixVersion
// FixVersion 2026.08.10.
// Related product-doc updates on force-offline and auto-upgrade are held for a separate pass.
// EPA-04 (KOB-54286) was still To Do at draft time (2026-08-27); a purple "newer deviceConnect
// version available" banner has since become present in the UI. Bullet updated per post-merge
// review to reflect current UI state.

== Kobiton MCP server: run-interactive-session on Windows and Linux

- The Kobiton MCP server's `run-interactive-session` skill now runs on Windows (x64) and Linux (x64) in addition to macOS. Apple Silicon Macs use the native `macos-arm64` build, removing the previous Rosetta 2 requirement.

// REVIEWER NOTE:
// KOB-54550 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-12 | bucket_source: updated_fallback
// bucket:updated — verify
// Kept under MCP filter as a major change (new platform support). Components: MCP server, Plugin.
// Intel macOS is the one regression — no macos-x64 artifact is published upstream. Confirm acceptable.
// Section trimmed during refinement — the following details were dropped as internal or off-topic:
//   - Binary unbundling and pinned download from `public.kobiton.download` (implementation detail;
//     users don't manage the cached binary themselves)
//   - Checksum verification, cache path (`~/.kobiton`), install-script mechanics
//   - "Install script runs at Claude Code session start" (Claude Code SessionStart hook plumbing)
//   - `/automate:doctor` version-drift check — user-visible in Claude Code, but not the focus of
//     this entry; users will encounter it through normal plugin use
//   - "Plugin never defaults to `latest`" pin policy (internal maintenance policy)
// FINAL: ok to include

== ADB shell on Public Cloud devices

On Public Cloud devices, `kobiton device adb-shell` supports a defined set of diagnostic commands, argument syntax, and file transfers from safe directories. Private Devices are unaffected.

*Highlights*

- Supported commands include `screencap`, `input`, and `settings get|put secure enabled_accessibility_services`. Other `settings` keys and subcommands are rejected.
- Supported read: `/proc/version`.
- Pipes, redirection, quoted arguments, and whitespace in arguments are supported.
- `pm install -r` works when the APK is staged in a safe directory, for example `pm install -r /data/local/tmp/app.apk`.
- File push (`PUT file:`) and pull (`GET file:`) support the safe directories `/sdcard/Download/`, `/sdcard/Documents/`, and `/data/local/tmp/`. Writes cannot target `/proc/version` or a safe directory itself.
- Denied transfers surface `AdbShellCommandRejectedException` instead of `SecurityException`. Customer automation that catches the old exception name should be updated.
- The `adb shell` toolbar in Portal manual sessions on Public Cloud Android devices uses the same policy as the CLI.
- The following actions are not supported: interactive shell, path traversal (`..`), oversized command lines, and unsafe write targets.
- `kobiton device adb-shell --help` documents the current policy, allowed commands, and permitted paths.

// REVIEWER NOTE:
// KOB-54468 + KOB-54472 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-10 | bucket_source: updated_fallback
// bucket:updated — verify
// Two tickets combined into one section since they cover the same restricted-ADB scope on the
// same components (CLI, deviceConnect).
// Behavior change on `SecurityException → AdbShellCommandRejectedException` is worth calling out
// to any customer automation that catches the old exception name.
// Section trimmed during refinement:
//   - Dropped "deny-by-default whitelist" framing — internal implementation, users don't see the
//     whitelist mechanism directly
//   - Dropped the historical "PUT was previously blocked / GET was previously unchecked" bullet
//     and the internal path-validation mechanics (control chars, metacharacters, `..` traversal,
//     safe-directory prefixes) — how the sanitizer works internally, not what the user needs
// FINAL: ok to include
//
// CONSOLIDATED at post-merge review (2026-09-04):
// Aug 28 ADB entries folded in here (KOB-54601 further-permissive expansion: pipes, redirection,
// quoted args, whitespace, broader commands/paths; KOB-54705 Portal manual-session toolbar parity
// on Public Cloud Android). Manager decision: one consolidated final-policy statement is clearer
// than a two-week chronology for a feature that only affects Public Cloud, and version specificity
// isn't important because Public Cloud customers cannot self-manage the deployment version.
// Also dropped "newly" framing throughout since the consolidated content spans two release weeks,
// and rewrote the "dangerous patterns" line as the neutral "The following actions are not supported:"
// list per manager wording preference.

== Automation sessions

- Fixed iOS automation Safari `driver.get()` failing with `Page.navigate not found` on iOS 26.4.2 and iOS 26.5. Safari navigation now works on both iOS versions.
- Fixed `SessionNotCreatedException (kAXErrorIPCTimeout)` and app launch taking more than two minutes when starting an XIUM automation session. Session creation and app launch now return in expected time.
- Fixed XIUM Android tests using `UiScrollable.scrollIntoView` returning `NoSuchElementException` without scrolling. The query now scrolls to the target element and returns it, matching the behavior on Basic Appium 2.
- When a Kobiton session is terminated, active client requests now include the termination reason in the response, instead of failing without context.

// REVIEWER NOTE:
// KOB-54067 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-13 | bucket_source: resolutiondate
// Component: Automation Session.
//
// KOB-54051 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-11 | bucket_source: resolutiondate
// Components: App Installation, Automation Session, XIUM.
//
// KOB-54483 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-12 | bucket_source: resolutiondate
// Fix shipped in build 2608.12 (2026-08-12). QA verified across 5 portal-test sessions.
// XIUM scrollIntoView; folded in from a dedicated XIUM section at post-merge review since XIUM is the default automation.
//
// KOB-52850 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-13 | bucket_source: updated_fallback
// bucket:updated (verify). Component: deviceConnect. Session-termination-reason API response;
// folded in from a dedicated deviceConnect section at post-merge review since the audience is automation clients.
// FINAL: ok to include

== Device passcode automation

- Fixed iPad passcode automation failing to complete on affected devices. Passcode entry now completes on iPad.
- Fixed passcode automation missing the first digit of the passcode. All digits are now entered.

// REVIEWER NOTE:
// KOB-54052 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-12 | bucket_source: resolutiondate
// Component: Passcode. iPad-specific.
//
// KOB-54486 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-12 | bucket_source: resolutiondate
// Component: Passcode. Missing-first-digit issue.
// FINAL: ok to include

== Kobiton CLI

- Fixed CLI session failing intermittently with `[MH-400] Failed to book device`. CLI sessions now start reliably against booked devices.

// REVIEWER NOTE:
// KOB-54428 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-05 (fixVersion 2026.08.15) | bucket_source: fixVersion
// Moved from Aug 7 page during compilation — resolutiondate is 08-05 but fixVersion is 08-15,
// which places it in the Aug 10-16 week. Placement confirmed during refinement.
// FINAL: ok to include

== Component versions

This release includes the following component versions:

* Portal 2608.15
* deviceConnect 2608.12
* Kobiton CLI 2608.11
* gigacap 2608.12
// Portal build confirmed by the release team at final review.
// Extracted from `dl.kobiton.download/deviceconnect/2608.122226.0+master.0b6752c/`,
// `public.kobiton.download/kobiton-cli/2608.111043.0+master.a74109a/`, and
// `dl.kobiton.download/gigacap/2608.121809.0+44cab34-1/` per the mass bulk-status comment
// posted across shipped tickets.
// FINAL: ok to include
