= Kobiton <next release version> Release Notes
:navtitle: Kobiton <next release version> release notes

// DRAFT release note generated by the docs-workflow (Robot Ranch) for review by the docs team.
// This page is intentionally NOT wired into release-notes/nav.adoc yet.
// Action for the docs team before publishing:
//   1. Assign the target release version and date (both are placeholders below).
//   2. Move this "Reporting" entry into that release's existing
//      "Bug fixes and improvements" section, then delete this standalone page.
//   3. Confirm the fix is deployed to the target environment. The change is merged
//      but takes effect only after the Usage Report dashboard is re-provisioned,
//      so it is not yet visible in production. Do not publish under a shipped
//      release until deployment is confirmed.

_<release date to be confirmed>_

== Bug fixes and improvements

=== Reporting

- Fixed an issue where value rows on some Usage Report summary cards could appear cut off beneath their column headers on certain displays.
