= Cloud September 10, 2026

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by the build date in YYYYMM.DD format. For example, 202609.10 represents a build created on September 10, 2026.

Cloud release notes are typically published weekly and summarize changes since the previous publication. +
Version information for individual software components is available in the Component versions section.
====

// Generated by The Gazette.
// Review structure, release date, and entry placement before publishing.

== Target release TBD

// Source: KOB-54737 from PR #585
// https://github.com/kobiton/docs/pull/585

== Kobiton MCP server

- Fixed an issue where the `getCredential` MCP tool returned an authorization error for MCP servers configured with API key authentication. The tool now returns your username, API key, and portal URL under both API key and OAuth authentication.

// Source: KOB-54809 from PR #584
// https://github.com/kobiton/docs/pull/584

== Kobiton MCP server

- Fixed the `listTestRuns` tool rejecting requests that sorted test runs by creation date. The tool now declares the sort keys it accepts, `CREATED_AT` and `NAME`, and requests that use the earlier lowercase spelling succeed.

// Source: KOB-54806 from PR #583
// https://github.com/kobiton/docs/pull/583

== AI and MCP

- New: the Kobiton `automate` plugin adds a `listTeams` tool that returns the teams you belong to. Organization members can now look up a team id and complete a test management list call that previously failed with `Team id is required`.
- The `teamId` descriptions on the test case, test suite, and test run tools now state the team scoping rules that the API enforces. Organization admins who omit `teamId` receive organization-level items only. Organization members pass a `teamId`. No list call returns items from more than one team.

// Source: KOB-54604 from PR #581
// https://github.com/kobiton/docs/pull/581

== API keys

- API keys on *Settings > API Keys* are now hidden by default. Select the eye icon next to a key to show its value, and select the icon again to hide it. Each key is controlled independently, and every key returns to the hidden state when you leave the page and open it again.
- The *Copy* action copies the real key value whether the key is hidden or shown.
- The API Keys page now includes a *Created* column showing the date each key was generated.

// REVIEWER NOTE: candidate bullet held back pending confirmation of whether the v1 user
// API is covered by the public API reference. Precedent from the 2026-08-28 page is that
// Portal-only endpoints are not documented publicly, so this is not included by default:
//
//   - `GET /v1/users/keys` now returns a `createdAt` timestamp for each key alongside the
//     existing `alias` and `key` fields. The change is additive; existing consumers are
//     unaffected.
//
// REVIEWER NOTE: the accessible name of the show/hide control is not confirmed in the
// source evidence. The wording above describes the control as "the eye icon", which is how
// the originating ticket describes it. Replace with the shipped label if one exists.

// Source: KOB-54909 from PR #578
// https://github.com/kobiton/docs/pull/578

== Device management

- Fixed the Device Management page failing to open. On a fresh page load it redirected to a "Can't find what you're looking for" error, and when opened from another Settings page it reported "You currently have no hosting machine" even though host machines were connected. This occurred only when a single host machine ran two or more devices that share a device name and report no operating system. Device Management now lists every connected host machine and its devices.

== Session Explorer

- Fixed the Overview tab showing `undefined` in the *Operating System* field for a device that reports no operating system. The field now shows only the OS version.

// Source: KOB-54740 from PR #572
// https://github.com/kobiton/docs/pull/572

- Fixed the *CLI for Mac*, *CLI for Windows*, and *CLI for Linux* links under *Downloads* in Kobiton Portal, which pointed to an outdated package location. Each link now resolves to the most recently published CLI build for its platform.

// Source: KOB-54741 from PR #571
// https://github.com/kobiton/docs/pull/571

=== Portal

- Fixed an issue where opening Kobiton Portal at its root URL hid features included in your subscription until you refreshed the page. The *virtual devices* tab was missing from the device list, and Kobiton CLI packages were missing from Downloads in the left navigation. Opening a Portal page by its direct URL was not affected.


== Phoenix candidates

// Jira tickets in "Wait For Deployment" / "Deployed" status when swept, plus a follow-up
// Jira sweep. Metadata enriched from Jira on 2026-09-10.
// Triage required: decide which of these warrant a release-note entry, then draft.
//
// Source files / sweeps:
// - phoenix-candidates 12.json (queried 2026-09-02)
// - phoenix-candidates 13.json (queried 2026-09-10)
// - Jira sweep 2026-09-10: project = KOB AND statusCategory = Done AND created > "2026-08-28"
//
// Excluded / removed at 2026-09-10 triage:
// - Already covered by drafted notes above: KOB-54809, KOB-54909, KOB-54806
// - Junk / obsoleted / QA-internal: KOB-54822, KOB-54891, KOB-54954
// - virtualUSB entries (out of scope): KOB-53968, KOB-23149
// - Internal-only work: KOB-54816 (ASP.NET rewrite), KOB-54814 (ObjC->Swift port),
//   KOB-54819 (Google Tag Manager marketing conversion)
// - Duplicate: KOB-54832 (dupes KOB-54806)
// - Closed-by-design MCP tickets: KOB-54807, KOB-54808 (KOB-54808 closed via KOB-54811;
//   both by-design, not shipping changes)
// - KOB-54630 (HarmonyOS app installation) — was moved to "Wait For Deployment" by accident;
//   correct state is "New", not shipping in this window
// - KOB-54970 (HarmonyOS .hap/.app upload rejection) — closed because the underlying feature
//   (KOB-54630) is not done, so the reported bug does not actually exist
// - KOB-54818 (GameDriver Turbo Test executor drop) — original diagnosis was wrong; Vinh
//   re-verified GameDriver end-to-end on Production against a GEM-hosted Android device with
//   an executor client in place, and it works (hosted test report and video included). Full
//   record in KOB-54954 (internal verification ticket)
// - KOB-55039 (GameDriver dotnet vstest run_command) — Deployed but not Released, so out of
//   scope for this window
// - HarmonyOS NEXT set (KOB-54634, KOB-54633, KOB-54632, KOB-54631) — internal foundation
//   work, matches last week's flagging. HarmonyOS is not exposed in Portal (no HarmonyOS
//   devices listed, no user-facing OS filter), so these entries are not customer-facing yet
//
// "Ship signal" = the release-versions the ticket shipped on, based on the deployment
// bulk-update comment posted on shipped tickets.

=== [XIUM][Android][iOS] Cached element click returns HTTP 404 no such element error after the underlying accessibility node is recreated; identical Basic Appium test succeeds

// Ticket: https://kobiton.atlassian.net/browse/KOB-54824
// Status: Released
// Type: Bug
// Resolution: Fixed
// Team: deviceConnect (Phoenix)
// Components: XIUM
// Deployment: Cloud (per customfield_10379)
// Created: 2026-09-02
// Updated: 2026-09-10
// Ship signal: deviceConnect 2609.1, gigacap 2609.1, Kobiton CLI 2609.4
// Source: Jira sweep 2026-09-10

=== iOS native alert "Shared All Contacts" not detected in element tree, blocks automated tests

// Ticket: https://kobiton.atlassian.net/browse/KOB-54760
// Status: Released
// Type: Bug
// Team: deviceConnect (Phoenix)
// Components: Element selection, Portal
// Created: 2026-08-28
// Updated: 2026-09-10
// Ship signal: deviceConnect 2609.1, gigacap 2609.1, Kobiton CLI 2609.4
// Source: Jira sweep 2026-09-10


== Component versions

This release includes the following component versions:

* deviceConnect 2609.1
* gigacap 2609.1
* Kobiton CLI 2609.4

// Versions confirmed for KOB-54824 at 2026-09-10 triage. Same versions apply to KOB-54760,
// which carries the same 2026-09-10 deployment bulk-update comment.
//
// REVIEWER NOTE: no new Portal version shipped this window. Slightly unexpected — call this
// out in the draft so the release reader knows Portal is unchanged, and verify at final review
// (KOB-54760 has Portal in its components, which is what surfaced the question).
//
// REVIEWER NOTE: no new deviceShare or kobiCTL versions this window — tracks with what's
// active. Omit per the 2026-08-28 precedent (components with no new build are not carried
// over on the versions list).
