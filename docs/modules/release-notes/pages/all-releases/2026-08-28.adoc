= Cloud August 28, 2026

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by the build date in YYYYMM.DD format. For example, 202608.28 represents a build created on August 28, 2026.

Cloud release notes are typically published weekly and summarize changes since the previous publication. +
Version information for individual software components is available in the Component versions section.
====

// Generated by The Gazette (catch-up compilation).
// This bucket is heavy and inflated: 24 of 32 remaining tickets rely on the `updated`-date fallback
// because they lack `resolutiondate`. Today's (2026-08-27) mass component-edit in Jira touched
// many of these and moved their `updated` timestamps to today, so several entries below may
// belong to earlier weeks. Verify each entry's actual ship date before publishing; move
// misplaced entries to the correct week page. Every fallback entry carries `// bucket:updated — verify`.
//
// Dropped from this bucket during compilation:
//   KOB-52788 (Standalone 4.26S docs — OOW, 4.26S shipped earlier)
//   KOB-52904, KOB-52905, KOB-52907 (Copilot CLI EPA — routine MCP-plugin doc updates)
//   KOB-52399 (MCP OAuth — routine MCP change)
//   KOB-54731 (Turbo Test backend performance — internal)
//   KOB-54638 (deviceConnect self-signed TLS cert — internal ops)
//   KOB-54724 (super-admin sensitive-session viewer — internal admin tool)
//   KOB-54295 (Auto-provision Superset dashboards — internal ops)
//   KOB-53389 (Appium AI Test Suite for Model Evaluation — internal QA)
// Dropped during refinement:
//   KOB-53041 / KOB-53042 / KOB-53043 / KOB-53044 / KOB-53045 (Portal auto-logout, EPA 1-5) —
//     already documented in the 4.28 release notes as "Portal Auto Logout (configurable session
//     timeout)". Same feature, same Org Settings toggle, same deferred-logout-for-active-sessions
//     behavior. The updated_fallback bucket landed the EPA tickets here on the 2026-08-27 mass
//     component-edit timestamp, but the feature shipped in 4.28 (2026-07-17).
//   KOB-51184 (Bluetooth Keyboard on iOS 26) — Bluetooth keyboard support and Bluetooth Bridge
//     pairing are documented in the 4.23 release notes. The area has not seen major changes
//     since; the assigned engineer has moved on to other work. Likely dragged into this bucket
//     by the 2026-08-27 mass component-edit timestamp. If a genuinely new iOS 26-specific
//     addition later needs documenting, treat it as a separate entry then.

// REMOVED at post-merge review (2026-09-04): entire Appium AI enhancements section removed.
// Reason: Cloud release notes cannot reference dc.ini. Only Standalone and Hybrid customers
// can edit dc.ini; Cloud customers do not access, see, or receive it (Kobiton manages Cloud
// deviceConnect). The last remaining bullet ("Full-resolution screenshots now sent to LLM by
// default; opt out via NaturalLanguageLocator.KeepOriginalScreenshot in dc.ini") violated
// this rule. The default change itself is also not actionable for Cloud users, who don't
// configure LLM setup — Kobiton chooses the defaults.
//
// All Appium AI tickets in this batch are Cloud AI Nova Engine changes. They may still be
// relevant for internal enablement docs but do not belong in customer-facing Cloud release
// notes. Prior refinement decisions preserved for compilation-workflow reference:
//
// KOB-54607, KOB-54608, KOB-54609, KOB-54610, KOB-54611, KOB-54613, KOB-54614, KOB-54645
// Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify (all). Component: AI Nova Engine.
//   - KOB-54607 (system prompt simplified) — internal prompt refactor + default Text/Vision
//     model switched to `gpt-5.6-terra`. Customer-invisible except for accuracy gains.
//   - KOB-54608 / KOB-54611 (Anthropic Claude + Kobiton-hosted LLM gateway) — customer-facing
//     wording described dc.ini setup that Cloud customers cannot perform.
//   - KOB-54609 + KOB-54610 (coord-space) — future-proofing per Tu's 2026-09-04 Slack response;
//     no current supported deployment (Qwen2.5-VL, Opus, GPT) requires customer action.
//   - KOB-54613 (LlmClient.Temperature removed / TextExtraBody migration) — Temperature was
//     server-side dc.ini only, never a session capability; nothing for external users to migrate.
//   - KOB-54645 (base-URL override API key) — audience is customers self-configuring LLM
//     providers, which is a Standalone/Hybrid concern, not Cloud.

// CONSOLIDATED at post-merge review (2026-09-04): ADB shell on Public Cloud devices section
// removed from this page and folded into the Aug 14 consolidated ADB entry. Manager decision:
// one final-policy statement is clearer than a two-week chronology for a feature that only
// affects Public Cloud. See Aug 14 page for the consolidated content.
// Removed tickets: KOB-54601 (further-permissive expansion), KOB-54705 (Portal manual-session
// toolbar parity).

== Reports

- New report: *Parallel Sessions over time*, which tracks concurrent session counts across a time range.

// REVIEWER NOTE:
// KOB-54356 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-24 | bucket_source: updated_fallback
// bucket:updated — verify. Component: Reporting.
// Final review: the one-line bullet stands as the release-note entry. Chart type, filters, and
// where to find the report in Portal belong in the Reports user guide, not here — follow up there.

== API v2

- The installed-applications API response now includes `app_version_id` alongside the existing device and application identifiers. The value matches `kobiton-store:v` from Appium scripts, so callers can compare installed apps against the intended version in the App Repository.

// REVIEWER NOTE:
// KOB-54284 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Component: API. Bullet expanded at post-merge review with `app_version_id`
// field name and `kobiton-store:v` equivalence per ticket confirmation.
// FINAL: ok to include
//
// REMOVED at post-merge review: KOB-53966 (New Portal API to retrieve the gRPC URL for the
// caller's session). Manager decision: Portal APIs are typically Portal-only, not documented in
// public API docs. If public API docs are not being updated for this endpoint, it isn't external.

== Kobiton CLI

- New: CLI sessions are now supported on Public Cloud devices. Previously, they could not start on Public Cloud devices.

// REVIEWER NOTE:
// KOB-54033 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Components: CLI, MCP server. Primary component is CLI.
//
// KOB-54477 (Direct Hub not propagating CLI failures for unsafe-directory pushes) — bullet dropped
// at final review: "Direct Hub" is not a term end users know, and the fix cannot be described
// without it. | Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Components: CLI, MCP server.

== Xiaomi device behavior

- Xiaomi devices now stay awake for the duration of long automation runs. Xiaomi's aggressive deep-idle behavior previously suspended sessions mid-run.

// REVIEWER NOTE:
// KOB-54023 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-24 | bucket_source: resolutiondate
// Component: deviceConnect. Xiaomi-specific device-idle behavior. Bullet reworded during
// refinement to drop `deviceidle`/`whitelist` mechanism-talk; framed by customer effect
// (device stays awake) rather than the Android system command used to fix it.
// Section renamed from `deviceConnect` to `Xiaomi device behavior` at post-merge review — describes
// what changed (Xiaomi-specific behavior) rather than the internal component that shipped the fix.
// FINAL: ok to include
//
// REMOVED at post-merge review: KOB-54704 (network-capture burst deadlocking upstream writes,
// freezing screenshots). Manager decision: internal framing (network-capture burst, upstream
// writes) and no clean user-visible rewrite of the customer-observable symptom.

== Component versions

This release includes the following component versions:

* Portal 2608.28
* deviceConnect 2608.26
* gigacap 2608.25
// Portal build confirmed at final review.
// Kobiton CLI is intentionally not listed — confirmed at final review that no new CLI version
// shipped in the Aug 24-28 window. Components with no new build are omitted, not carried over.
// Extracted from `dl.kobiton.download/deviceconnect/2608.260938.0+master.e07c6d0/` and
// `dl.kobiton.download/gigacap/2608.251726.0+239b981-1/` per the mass bulk-status comment
// posted across shipped tickets.
