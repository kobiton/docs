= Cloud August 28, 2026

_August 28, 2026_

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by date. Build numbers use a format such as 202608.28, and release notes display the corresponding date as August 28, 2026.
====

// Generated by The Gazette (catch-up compilation).
// This bucket is heavy and inflated: 24 of 32 remaining tickets rely on the `updated`-date fallback
// because they lack `resolutiondate`. Today's (2026-08-27) mass component-edit in Jira touched
// many of these and moved their `updated` timestamps to today, so several entries below may
// belong to earlier weeks. Verify each entry's actual ship date before publishing; move
// misplaced entries to the correct week page. Every fallback entry carries `// bucket:updated — verify`.
//
// Dropped from this bucket during compilation:
//   KOB-52788 (Standalone 4.26S docs — OOW, 4.26S shipped earlier)
//   KOB-52904, KOB-52905, KOB-52907 (Copilot CLI EPA — routine MCP-plugin doc updates)
//   KOB-52399 (MCP OAuth — routine MCP change)
//   KOB-54731 (Turbo Test backend performance — internal)
//   KOB-54638 (deviceConnect self-signed TLS cert — internal ops)
//   KOB-54724 (super-admin sensitive-session viewer — internal admin tool)
//   KOB-54295 (Auto-provision Superset dashboards — internal ops)
//   KOB-53389 (Appium AI Test Suite for Model Evaluation — internal QA)

== Portal auto-logout (organization setting)

*Highlights*

- Portal now supports an organization-level auto-logout policy configured in Org Settings. Admins can enable the toggle and pick a timeout from a duration dropdown.
- Active sessions are respected. When the timeout hits, Portal defers the logout until all in-progress sessions complete.
- On expiry, the session-expiry handler redirects to the login page with an expiry message and a deep-link back to the page the user was on.
- The timeout is enforced in the Portal auth middleware, so it applies across surfaces that share the same session.

// REVIEWER NOTE:
// KOB-53041 (EPA 1 UI) + KOB-53042 (EPA 2 backend store) + KOB-53043 (EPA 3 middleware enforcement)
// + KOB-53044 (EPA 4 frontend expiry handler) + KOB-53045 (EPA 5 deferred logout for active sessions)
// Team: DLM | Deployment: Cloud | ship_date: 2026-08-26 | bucket_source: updated_fallback
// bucket:updated — verify. Component: Settings.
// Five EPA tickets grouped into one section since they ship the same feature.

== Appium AI enhancements

*Highlights*

- Added support for Kobiton-hosted LLM at `llm.kobiton.com`.
- Added support for the Anthropic LLM client.
- Added override capabilities for vision coordinate mode.
- Full-resolution screenshots are now preserved when sending to the LLM. Previously screenshots were downscaled before being sent.
- Vision responses now support two common bbox conventions: Pixel and Normalized 0-1000.
- LLM API keys inherited server-side are only applied when the endpoint is server-controlled (security tightening — prevents leaking a server-side key to a customer-controlled endpoint).
- System prompt simplified for compatibility with newer LLMs.
- `LlmClient.Temperature` removed; the client now supports model-specific request parameters instead.

// REVIEWER NOTE:
// KOB-54607, KOB-54608, KOB-54609, KOB-54610, KOB-54611, KOB-54613, KOB-54614, KOB-54645
// Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Component: AI Nova Engine.
// Eight enhancement tickets grouped into one section (same component, shipped together per updated timestamps).

== Restricted ADB shell — allow list retired, deny list active

The restricted ADB shell used on public cloud and trial devices has been re-architected from an allow list into a deny list. This is a significant policy shift that follows the whitelist-expansion + safe-path work from the August 14 release.

*Highlights*

- Access is now deny-based: commands are permitted unless explicitly rejected. Previously the whitelist blocked anything not explicitly permitted.
- The rejection surface still covers the same abuse classes (interactive shell, pipes/redirection, shell metacharacters, `..` traversal, oversized command lines, unsafe write targets).
- Fixed `adb shell` toolbar in Portal manual sessions bypassing the restricted-shell protection on public Android devices. The toolbar now goes through the same sanitizer as the CLI.

// REVIEWER NOTE:
// KOB-54601 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-24 | bucket_source: updated_fallback
// bucket:updated — verify. Components: CLI, deviceConnect. Policy shift — customer-facing behavior change.
//
// KOB-54705 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-24 | bucket_source: updated_fallback
// bucket:updated — verify. Components: deviceConnect, Manual Session.

== Reports

- New report: *Parallel Sessions over time* — tracks concurrent session counts across a time range.

// REVIEWER NOTE:
// KOB-54356 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-24 | bucket_source: updated_fallback
// bucket:updated — verify. Component: Reporting.
// TODO: confirm chart type, filters, and access location in Portal before publish.

== API v2

- The installed-applications API now returns the App Version ID alongside the application identifiers.
- New Portal API to retrieve the gRPC URL for the caller's session.

// REVIEWER NOTE:
// KOB-54284 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Component: API.
//
// KOB-53966 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Component: API v2.

== Kobiton CLI

- New: CLI sessions are now supported on public cloud devices. Previously public cloud restricted CLI session starts.
- Fixed the Direct Hub not propagating CLI failures for unsafe-directory pushes. These failures were silently hidden — they are now surfaced to the caller, matching the CLI's error-propagation fix from earlier releases.

// REVIEWER NOTE:
// KOB-54033 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Components: CLI, MCP server. Primary component is CLI.
//
// KOB-54477 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Components: CLI, MCP server.
// Draft mirrored from PR #566's "Kobiton CLI reports refused file pushes" section content
// but simplified — the fully-elaborated version from PR #566 can replace this bullet if desired.

== Bluetooth Keyboard for iOS 26 and unsupervised passcodes

- Added Bluetooth Keyboard support on iOS 26 devices, including passcode entry on unsupervised devices. Previously the Bluetooth Bridge did not cover iOS 26 or unsupervised-device passcode entry.

// REVIEWER NOTE:
// KOB-51184 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-14 (per updated), older status "Released" per prior candidate file | bucket_source: updated_fallback
// bucket:updated — verify. Components: bluetoothBridge, deviceConnect.
// This ticket was in an early candidate file with status "Deployed" as of 2026-08-14 — it may
// actually belong on the Aug 14 page. Confirm.

== deviceConnect

- Fixed a network-capture burst deadlocking a session's upstream writes and freezing screenshots.
- Added a Xiaomi deviceidle whitelist so Xiaomi devices no longer enter deep idle during long automation runs.

// REVIEWER NOTE:
// KOB-54704 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-27 | bucket_source: updated_fallback
// bucket:updated — verify. Component: deviceConnect.
//
// KOB-54023 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-24 | bucket_source: resolutiondate
// Component: deviceConnect. Xiaomi-specific device-idle behavior.

////
// virtualUSB — held for future release, do not publish yet
== virtualUSB

- Fixed unnormalized login error messages: GUI printed raw JSON, CLI dumped raw Rust/HTML internals (held).
- Fixed CLI login failure printing the raw internal error object instead of a user-facing message (held).
- Fixed macOS GUI showing a generic "server is temporarily unavailable" for every non-400/401 login failure, discarding the server's reason and CLI guidance (held).
- Fixed iOS 17+ Core Device not being released after CLI Disconnect on macOS + CLI — device stuck "Utilizing"/"device is preparing" (held).
- Fixed macOS PKG installer being unsigned and the app bundle being un-notarized; Gatekeeper warned "Apple could not verify… free of malware" (held).

// REVIEWER NOTES (virtualUSB held):
// KOB-54641 | Phoenix | Cloud | 2026-08-24 | resolutiondate | virtualUSB_hold
// KOB-54595 | Phoenix | Cloud | 2026-08-26 | resolutiondate | virtualUSB_hold
// KOB-54734 | Phoenix | Cloud | 2026-08-26 | resolutiondate | virtualUSB_hold
// KOB-54592 | Phoenix | Cloud | 2026-08-27 | updated_fallback | virtualUSB_hold
// KOB-53088 | Phoenix | Cloud | 2026-08-27 | updated_fallback | virtualUSB_hold
////

== Component versions

This release includes the following component versions:

// TODO: confirm Portal build for the 2026-08-28 release window. No ticket in this bucket has a
// fixVersion in the Aug 24-28 range.
// TODO: confirm deviceConnect version — Phoenix tickets in this bucket have no fixVersion.
