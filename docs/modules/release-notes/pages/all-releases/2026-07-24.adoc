= Cloud July 24, 2026

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by the build date in YYYYMM.DD format. For example, 202607.24 represents a build created on July 24, 2026.

Cloud release notes are typically published weekly and summarize changes since the previous publication. +
Version information for individual software components is available in the Component versions section.
====

// Generated by The Gazette (catch-up compilation).
// Review structure, release date, and entry placement before publishing.
// This is the first weekly page after the 2026-07-17 (4.28) cutover to date-based versioning.
// Dropped from this bucket during compilation:
//   KOB-52716 (deviceConnect abstract exhaust capture — internal refactor)
//   KOB-52321 (gigacap DTLS RSA → EC P-256 — internal crypto swap, no user-visible behavior)
//   KOB-52322 (gigacap trickle-ICE SDP fix — internal WebRTC signaling)

// REMOVED during final review:
//   KOB-53790 (Convert automation session to Test Case — Revisit support) — already documented in
//   the 4.28 release notes. It remains the foundation the MIXED Revisit work on KOB-53798
//   (Aug 7 page) builds on, but it does not warrant a second entry here.

== Appium

- Basic Appium now enables all plugins by default. Previously, plugins had to be enabled per test.

// REVIEWER NOTE:
// KOB-54065 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: changelog
// Component: Appium.
// FINAL: ok to include

== Automation session fixes

- Fixed Basic Appium no-app sessions failing every command with `Unsupported URI`. The session never registered because attached sessions were skipping Launch. Attached no-app sessions now register and accept commands.
- Added support for Flutter Appium Driver sessions on iOS 17+.

// REVIEWER NOTE:
// KOB-54066 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-07-21 | bucket_source: resolutiondate
// Component: deviceConnect.
//
// KOB-52934 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-22 | bucket_source: resolutiondate
// Component: Flutter.
// FINAL: ok to include

== API v2

- New endpoint to fetch a single app version by ID: `GET /v2/apps/{appId}/versions?app_version_id=<id>`.
- Fixed `Terminate By UDID` on the Reservations API returning the terminated UDID in the `failed` list, duplicated per device record. Successful terminations are no longer misreported.
- API v2 now surfaces database errors when the host is low on disk space.

// REVIEWER NOTE:
// KOB-54048 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-20 | bucket_source: changelog
// Component: API v2.
//
// KOB-54269 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-20 | bucket_source: resolutiondate
// Component: API v2.
//
// KOB-54018 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-23 | bucket_source: resolutiondate
// Components: API, core-service.
// FINAL: ok to include

== Kobiton CLI

- The CLI now retries transient connection failures automatically, so a brief network interruption no longer fails the operation.

// REVIEWER NOTE:
// KOB-53228 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: updated_fallback
// Component: CLI.
// PLACEMENT NOTE: fixVersion on this ticket is `2026 Q2 - 4.27` (shipped July 17, 2026 as 4.28).
// This entry belongs on the 4.28 release notes page but was not documented there. Landed here so the
// change is captured somewhere in user-facing docs. Do not retroactively edit 4.28.
// Per final review: retry-window figures are deliberately not published, and the file-pull
// transient-error (gRPC/tonic) bullet was dropped as too technical for a user-facing note.

// REMOVED during final review:
//   KOB-52807 (Device cleanup policy — `cfgutil erase`/`prepare`/`install-profile` via
//   `MCInstallCommand`) — cleanup-policy operations are only useful internally; no user-facing entry.

== Kobiton MCP Plugin

- The Kobiton MCP Plugin now includes a new-user onboarding surface. First-time users get a guided setup instead of jumping straight into the tool list.

// REVIEWER NOTE:
// KOB-54046 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-22 | bucket_source: changelog
// Components: MCP server, Plugin. Kept under MCP filter as major (new onboarding surface).
// FINAL: ok to include

== deviceConnect Console

- Bluetooth server errors now surface in the deviceConnect Console.

// REVIEWER NOTE:
// KOB-53978 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: changelog
// Component: deviceConnect.
// FINAL: ok to include

// NO COMPONENT VERSIONS SECTION on this page, by decision at final review: the packages shipped
// this week are still marked 4.28 and the weekly release-versioning format had not been introduced
// yet. Do not add a "Component versions" section here.
