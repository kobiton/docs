= Cloud July 24, 2026

_July 24, 2026_

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by date. Build numbers use a format such as 202607.24, and release notes display the corresponding date as July 24, 2026.
====

// Generated by The Gazette (catch-up compilation).
// Review structure, release date, and entry placement before publishing.
// This is the first weekly page after the 2026-07-17 (4.28) cutover to date-based versioning.
// Dropped from this bucket during compilation:
//   KOB-52716 (deviceConnect abstract exhaust capture — internal refactor)
//   KOB-52321 (gigacap DTLS RSA → EC P-256 — internal crypto swap, no user-visible behavior)
//   KOB-52322 (gigacap trickle-ICE SDP fix — internal WebRTC signaling)

== Convert automation session to Test Case — Revisit support

*Highlights*

- Test Cases converted from an automation (Appium) session can now be revisited end-to-end. The Revisit path replays each converted step through the automation-command-replay executor and reports per-step results.
- Steps with no replayable equivalent (for example, an unsupported Appium endpoint) are skipped with a reason. The Revisit run continues rather than aborting.

// REVIEWER NOTE:
// KOB-53790 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: updated_fallback
// bucket:updated — verify. Component: Test Management.
// This is the foundation the MIXED Revisit work on KOB-53798 (Aug 7 page) builds on.
// FINAL: remove because already mentioned in 4.28 release notes

== Appium

- Basic Appium now enables all plugins by default. Previously plugins had to be enabled per test.

// REVIEWER NOTE:
// KOB-54065 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: changelog
// Component: Appium.
// FINAL: ok to include

== Automation session fixes

- Fixed Basic Appium no-app sessions failing every command with `Unsupported URI`. The session never registered because attached sessions were skipping Launch. Attached no-app sessions now register and accept commands.
- Fixed Flutter Appium Driver session creation failing on iOS 17+ with `Launching the application in debug mode for iOS 17 is not yet supported`. Flutter sessions now start on iOS 17+.

// REVIEWER NOTE:
// KOB-54066 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-07-21 | bucket_source: resolutiondate
// Component: deviceConnect.
//
// KOB-52934 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-22 | bucket_source: resolutiondate
// Component: Flutter.
// FINAL: ok to include

== API v2

- New: `GET /v2/apps/{appId}/versions?app_version_id=<id>` — a safe query-param variant to fetch a single app version by ID.
- Fixed `Terminate By UDID` on the Reservations API returning the terminated UDID in the `failed` list, duplicated per device record. Successful terminations are no longer misreported.
- Database errors are now surfaced when the host is low on disk space. Previously these failed silently.

// REVIEWER NOTE:
// KOB-54048 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-20 | bucket_source: changelog
// Component: API v2.
//
// KOB-54269 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-20 | bucket_source: resolutiondate
// Component: API v2.
//
// KOB-54018 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-23 | bucket_source: resolutiondate
// Components: API, core-service.
// FINAL: ok to include

== Kobiton CLI

- The CLI now retries transient connection failures automatically. Built-in retries were added to resource connections so a brief network blip no longer fails the operation outright.
- File-pull transient-error detection was broadened to catch additional gRPC (tonic) errors that were previously treated as terminal.

// REVIEWER NOTE:
// KOB-53228 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: updated_fallback
// Component: CLI.
// PLACEMENT NOTE: fixVersion on this ticket is `2026 Q2 - 4.27` (shipped July 17, 2026 as 4.28).
// This entry belongs on the 4.28 release notes page but was not documented there. Landed here so the
// change is captured somewhere in user-facing docs. Do not retroactively edit 4.28.
// TODO: unverified detail — a "~600ms retry window" figure surfaced during review; confirm whether
// retries are count-capped, time-bounded (e.g. ~600ms window), or both, and whether the behavior is
// configurable. Check with Chris Faulhaber (reporter/assignee) before publish.
// FINAL: ok to include. Do not need to mention the 600s retry windows. Also remove the second point as it is too technical to mention file-pull transient error.

== Device cleanup policy

- The device cleanup policy now supports `cfgutil erase`, `prepare`, and `install-profile` operations via `MCInstallCommand`. Cleanup runs can now use these to fully reset and re-provision iOS devices between sessions.

// REVIEWER NOTE:
// KOB-52807 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: changelog
// Component: Cleanup Policy.
// FINAL: remove as they are only useful internally.

== Automate plugin

- The automate plugin now includes a new-user onboarding surface. First-time users get a guided setup instead of jumping straight into the tool list.

// REVIEWER NOTE:
// KOB-54046 | Team: DLM | Deployment: Cloud | ship_date: 2026-07-22 | bucket_source: changelog
// Components: MCP server, Plugin. Kept under MCP filter as major (new onboarding surface).
// FINAL: ok to include

== deviceConnect

- Bluetooth server errors now surface in the deviceConnect admin UI. Previously admins had no visibility into Bluetooth server failures.

// REVIEWER NOTE:
// KOB-53978 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-07-24 | bucket_source: changelog
// Component: deviceConnect.
// FINAL: ok to include

== Component versions

This release includes the following component versions:

// TODO: no build URL found in ticket comments for this week — ask release team.
// The comment-scan across this bucket's tickets turned up no `dl.kobiton.download` or
// `public.kobiton.download` URLs, so Portal, deviceConnect, and Kobiton CLI versions cannot
// be extracted automatically. This is the first weekly page after the 4.28 cutover — the
// mass-status comment convention may not have been in place yet.
// FINAL: TODO
