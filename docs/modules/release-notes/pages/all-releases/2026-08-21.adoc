= Cloud August 21, 2026

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by the build date in YYYYMM.DD format. For example, 202608.21 represents a build created on August 21, 2026.

Cloud release notes are typically published weekly and summarize changes since the previous publication. +
Version information for individual software components is available in the Component versions section.
====

// Generated by The Gazette (catch-up compilation).
// Review structure, release date, and entry placement before publishing.
//
// Standalone deferred (no weekly Standalone cadence — held for next Standalone release):
//   KOB-54549 | Team: DLM | Deployment: Standalone | ship_date: 2026-08-17 | bucket_source: resolutiondate
//     [Standalone] Cannot log into Kibana on Updater-based deployments. Components: Installer, Standalone.
//     Not drafted here — Standalone releases are ad-hoc pending Updater finalization. When
//     the next Standalone release ships, this ticket + any relevant Cloud items from this window
//     should be bundled into a comprehensive Standalone release note.
//
// Dropped from this bucket during compilation:
//   KOB-54264 (Portal version display update, fixVersion 2026.07.22 — OOW)
//   KOB-43481 (Test Step action name display, fixVersion 2026.07.29 — moved to Jul 31 page)
// Also condensed as backend-only (kept as REVIEWER NOTE, not rendered):
//   KOB-54260 (EPA-03 remove offlineExclusionList backend mechanism — no user-visible change)
//   KOB-54423 (EPA-05 point api's auto-upgrade at dl.kobiton.download — URL change, no user-visible behavior)
//   KOB-52733 (deviceConnect general exhaust writer — internal telemetry plumbing)
//   KOB-54637 (Update deviceConnect nginx — infra, no user-visible change)
//   KOB-44438 (Remove dC support for upstreaming archive media via gRPC — dead-code cleanup;
//     per ticket description "we no longer have ever desire to upstream video via gRPC. Thus the
//     code that supports this is now dead code in dC and should be removed for simplification."
//     Nothing in production relied on it; there is no customer migration path. Prior draft
//     incorrectly framed this as a customer-facing deprecation.)
//   KOB-54639 (InstrumentsClient.WriteMessage NRE + leaked InstrumentsServiceObject on
//     stream-disposed-mid-write) — broken-teardown marker; per the ticket the observed 8 leaks
//     over ~19h "will not exhaust resources on its own" and "has not been shown to cause the
//     watchdog restarts on that host." No proven customer symptom; internal hygiene.

== Device Management

- Device Management now displays the deviceConnect version for each host machine. Previously, some host machines showed `N/A`.
- Host machines that were forced offline because of version lag come back online automatically.

// REVIEWER NOTE:
// KOB-54286 (EPA-04) | Team: DLM | Deployment: Cloud | ship_date: 2026-08-18 | bucket_source: updated_fallback
// bucket:updated — verify. New informational replacement banner (Org Admin only); banner copy
// preserved verbatim per KOB-54286 acceptance criteria.
//
// KOB-54259 (EPA-01) | Team: DLM | Deployment: Cloud | ship_date: 2026-08-18 | bucket_source: updated_fallback
// bucket:updated — verify. Accepts any version string (removes the strict format validation that
// caused new date-based versions to show as N/A).
//
// KOB-54056 (parent) | Team: DLM | Deployment: Cloud | ship_date: 2026-08-17 | bucket_source: fixVersion (2026.08.17)
// Parent epic — the Auto Upgrade Feature rework that removed force-offline enforcement (shipped on
// the Aug 14 page as KOB-48664 / EPA-02) and replaced it with this informational banner plus
// version-format acceptance.
//
// Held as backend-only (see file-top notes): KOB-54260 (EPA-03 offlineExclusionList removal —
// code-level only, no user-visible change), KOB-54423 (EPA-05 auto-upgrade source change from
// Nexus to R2 — internal distribution migration, no user-visible behavior).
// Section rewritten during refinement — stripped EPA-01/EPA-04 internal codes, "R2"/"Nexus"
// distribution names, "non-punitive" engineering framing, and the self-referential "parent epic
// wraps up" project-management flavor. Auto-upgrade mechanics bullet dropped — behavior is
// unchanged from the customer's perspective (source changed under the hood).
// Heading renamed from `Host machines` to `Device Management` at post-merge review — Device
// Management is the specific admin page customers know, more customer-friendly than the generic
// "Host machines". Both bullets are host-machine behavior on Device Management.
// Final review: version-display bullet cut back to the observable outcome (version shows instead
// of N/A) — the old vs. new version-string format and why it broke display are not the user's
// concern. The KOB-54286 upgrade banner bullet (Org Admin "Version X is available…" notice with
// Upgrade Now / dismiss) was dropped as low impact; heading narrowed to match. Third bullet kept.

== API v2 — Terminate reservation by device UDIDs

- `DELETE /v2/reservations/terminate` now accepts an optional set of device UDIDs alongside `reservation_ids`. Callers can end a reservation for specific devices without terminating the whole reservation set. UDIDs with no active reservation are reported in the response's `failed` list. Requests that pass only `reservation_ids` are unchanged. Authorization is unchanged — only reservations the caller has permission to terminate are affected.

// REVIEWER NOTE:
// KOB-54045 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-18 | bucket_source: updated_fallback
// bucket:updated — verify. Component: API v2.
// Endpoint + request shape confirmed from lead-engineer comment on ticket.
// FINAL: ok to include

== Kobiton CLI

- Fixed CLI-driven public-device sessions not counting session minutes toward usage when the session hit its timeout and was terminated. Usage now records these minutes.
- Fixed the CLI writing the pre-signed S3 upload URL and the session JWT to client-visible output. These credentials are no longer logged.
- New: create a user from the Kobiton CLI with `kobiton org user create` (wraps `POST /v2/organizations/members`). Required: `--email`, `--username`. Choose one of `--password` or `--enable-sso` (mutually exclusive). Optional: `--first-name`, `--last-name`. To keep the password out of shell history, supply it via the `KOBITON_NEW_USER_PASSWORD` environment variable. The caller's role must have `org_management.modify` permission.

// REVIEWER NOTE:
// KOB-54262 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-17 | bucket_source: updated_fallback
// bucket:updated — verify. Component: CLI.
//
// KOB-54478 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-18 | bucket_source: resolutiondate
// Security fix — pre-signed URL + JWT leak. Components: CLI, MCP server.
//
// KOB-54547 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-20 | bucket_source: updated_fallback
// bucket:updated — verify. Component: CLI. New capability.
// Command syntax + env-var + permission requirement confirmed from lead-engineer comment on ticket.
// FINAL: ok to include

== Device availability

- Fixed public and shared devices staying unavailable (`MH-400 ... is being used for CLI testing`) after a CLI session ended abnormally — for example a session timeout or a dropped connection. Devices now return to the available pool automatically, including any device still held past its expected session duration.

// REVIEWER NOTE:
// KOB-54584 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-17 | bucket_source: updated_fallback
// bucket:updated — verify. Component: Device Hub.
// Rewritten during refinement — dropped internal mechanics (`getErrorMessage` crash,
// stream-end fallback, TTL). Kept the user-visible error string `MH-400 ... is being used for CLI
// testing` since customers see it. Reference incident: 9+ shared sandbox devices locked during
// KOB-54477 verification, all recovered only via device-hub restart.
// Final review: all "Device Hub" wording removed (heading and bullet) — not a term users know.
// Framed as a device-availability fix; the operator-side recovery step (restarting device-hub) and
// the background-service-deploy trigger were dropped for the same reason.

== Apps and app storage

- Fixed `GET /v2/apps/{appId}` omitting `latest_version` when the request sets `includeAllAppVersions=true`. The response now returns `latest_version` alongside `versions`, with the same value the endpoint returns when `includeAllAppVersions` is omitted or set to `false`.
- `latest_version` skips versions that are still importing or parsing. An app with no other versions returns no `latest_version`.

// REVIEWER NOTE:
// KOB-53962 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-20 | bucket_source: fixVersion (2026.08.20)
// Components: Appium, Apps Repo, MCP server.
// FINAL: ok to include
//
// DROPPED at post-merge review: the `getApp` MCP tool bullet ("Fixed the getApp MCP tool returning
// latest_version as null when called with includeAllVersions=true"). Routine MCP tool fix that
// mirrors the API v2 fix above. Per house rule, routine MCP tool tweaks are omitted from release
// notes; MCP is called out only when the change is substantive on its own.

== Device passcode

- Fixed some iOS devices failing to start sessions with a passcode-related initialization error. Affected devices now start sessions as expected.

// REVIEWER NOTE:
// KOB-54594 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-19 | bucket_source: resolutiondate | Customer: EJ
// Moved from a dedicated deviceConnect section at post-merge review; bullet rewritten to describe
// the user-visible outcome (some iOS devices failing to initialize) instead of internal
// deviceControl / mobile_image_mounter mechanics.
// FINAL: ok to include
//
// REMOVED at post-merge review: KOB-54640 (macOS service-recovery / WatchDog).
// Manager decision: internal service plumbing, not user or admin facing.
//
// REMOVED at post-merge review: KOB-54676 (adb shell reboot bootloader restricted-shell bypass).
// Manager decision: likely a regression from the Aug 14 restricted-ADB-shell work. Fixing our
// own regression is not worth calling out as a separate release-note entry.

== Image Injection (Android)

- Fixed Android Image Injection failing with `Please Ensure Your Application Integrates Kobiton SDK` after the instrumented install reported success. Injection now proceeds after successful instrumented installs.

// REVIEWER NOTE:
// KOB-54466 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-21 | bucket_source: updated_fallback
// bucket:updated — verify. Component: Image Injection.
// FINAL: ok to include

== Appium automation

- Fixed XIUM not dismissing the iOS notification permission alert after Accept Alert. XIUM now dismisses the alert as expected.

// REVIEWER NOTE:
// KOB-54484 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-21 | bucket_source: updated_fallback
// bucket:updated — verify. Component: XIUM.
// FINAL: ok to include

== Component versions

This release includes the following component versions:

* Portal 2608.20
* deviceConnect 2608.20
* Kobiton CLI 2608.19
// Portal build confirmed by the release team at final review.
// gigacap is intentionally not listed, per final review: no gigacap change shipped to customers in
// the Aug 17-21 window. No gigacap URL was posted in this bucket's bulk-status comments.
// Extracted from `dl.kobiton.download/deviceconnect/2608.200116.0+master.1c336f4/` and
// `public.kobiton.download/kobiton-cli/2608.191335.0+master.1e8f487/` per the mass bulk-status
// comment posted across shipped tickets.
// FINAL: ok to include
