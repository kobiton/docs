= Cloud August 7, 2026

_August 7, 2026_

[NOTE]
====
Kobiton now uses date-based release versioning. Instead of version numbers such as 4.26S or 4.19.3, releases are identified by date. Build numbers use a format such as 202608.07, and release notes display the corresponding date as August 7, 2026.
====

// Generated by The Gazette (catch-up compilation).
// Review structure, release date, and entry placement before publishing.
// Dropped from this bucket during compilation: KOB-52866 (fixVersion 2026.06.16 — OOW),
// KOB-53040 (fixVersion 2026.06.15 — OOW), KOB-52491 (fixVersion "2026 Q2 - 4.26" — OOW),
// KOB-54428 (fixVersion 2026.08.15 — moved to Aug 14 page).

== Convert a mixed session into a Test Case

Completed mixed sessions can now be converted into Test Cases, extending the flow already available for manual and Appium automation sessions. The converted Test Case holds the manual steps and the automation steps in the order they were recorded, so a combined flow can be rerun without recreating the session.

*Highlights*

- A mixed session must be complete before it can be converted. Sessions that are still running are not eligible.
- The Test Case preserves the original recorded order across the manual and automation steps, and tags each step with its source.
- Convert from Session Explorer or from the *+New Test Case* pop-up on the Test Cases list page. Search for eligible sessions with `type:mixed`.
- Test Cases converted from a mixed session show *Mixed* as the Source type.
- A Test Run replays each step through the execution path that matches its source and reports per-step results. Steps with no replayable equivalent, such as an unsupported Appium endpoint, are skipped with a reason and the run continues.
- Appium script generation is not available for Test Cases converted from mixed sessions.

// REVIEWER NOTE:
// KOB-53798 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-06 | bucket_source: updated_fallback
// bucket:updated — verify

== Device details

- Fixed the *Apps* tab of the device modal reporting the newest version in the app repository as *Installed* instead of the build actually installed on the device. Android devices were affected when several builds of an app shared the same version.
- The app row on the *Apps* tab now shows *Installed* when any version of the app is installed, and names the installed build. Previously the row reported the status of the newest version in the app repository.
- When the build on the device cannot be matched to a version in the app repository, no version is marked *Installed*. Previously the newest version in the app repository was marked *Installed* in this case.

// REVIEWER NOTE:
// KOB-54289 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-05 | bucket_source: fixVersion
// Draft reused from PR #566 (verbatim), restored to roster after [MVW] filter drop. Component: dlm-background-services. FixVersion 2026.08.05.

== Accessibility

- Fixed VoiceOver and TalkBack audio being inaudible until the user interacted with the device screen. The Portal now starts WebRTC video playback muted and unmutes once the stream attaches, bypassing the browser autoplay restriction that was silencing the stream on session start.
- The WebRTC debug panel has been improved and can be enabled via `?webrtcDebugPanelEnabled=true` for diagnostics.

// REVIEWER NOTE:
// KOB-52004 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-06 | bucket_source: resolutiondate
// Fix migrated the Portal from the deprecated onaddstream API to ontrack (dropped webrtc-adapter shim).
// Components: Android Talkback, iOS VoiceOver, Lightning Mode, Portal.

== deviceConnect

- Fixed a stalled Password-Protected iOS device causing every subsequent CoreDevice tunnel reset to stall. Recovering the stalled device no longer blocks unrelated tunnel operations.
- Fixed the deviceConnect *System* page returning *Internal Server Error* because `/Admin/Poll` failed to serialize the device list. The endpoint now returns the device list successfully.

// REVIEWER NOTE:
// KOB-54391 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-06 | bucket_source: resolutiondate
// Customer report: AT&T.
//
// KOB-54424 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-06 | bucket_source: resolutiondate
// deviceConnect /Admin/Poll serialization fix.

== Kobiton Console (macOS)

- Fixed the Kobiton Console on macOS rejecting valid usernames, passwords, and API keys at the sign-in screen. Users can now sign in with any accepted credential type.

// REVIEWER NOTE:
// KOB-54439 | Team: Phoenix | Deployment: Cloud | ship_date: 2026-08-06 | bucket_source: resolutiondate
// Component: device Connect Console.

== Emulator and Simulator sessions

- Fixed Android emulator Portal manual sessions failing to establish and ending after approximately two minutes. Sessions now establish and remain active.

// REVIEWER NOTE:
// KOB-54332 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-06 | bucket_source: resolutiondate

== Device Inspector

- Fixed Inspector on Session Explorer not displaying the application UI hierarchy for XIUM sessions on iOS and Android devices. The hierarchy now renders for XIUM sessions on both platforms.

// REVIEWER NOTE:
// KOB-52533 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-07 | bucket_source: resolutiondate
// Customer report: EJ.

== Device passcode

- Fixed a passcode-requirement issue that prevented XCtest from being enabled on iOS devices. XCtest can now be enabled without the passcode blocking the workflow.

// REVIEWER NOTE:
// KOB-54043 | Team: DLM | Deployment: Cloud | ship_date: 2026-08-07 | bucket_source: resolutiondate
// Customer report: AT&T.

== Component versions

This release includes the following component versions:

* Portal 2608.06
// TODO: confirm Portal build — inferred from KOB-52004 and KOB-53798 fixVersions (2026.08.06).
// TODO: confirm deviceConnect version — Phoenix tickets in this bucket have no fixVersion.
// TODO: confirm device Connect Console version — KOB-54439 has no fixVersion.
