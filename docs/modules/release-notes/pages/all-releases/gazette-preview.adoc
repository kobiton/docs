= Release Notes

_Target release date TBD_

// Generated by The Gazette.
// Review structure, release date, and entry placement before publishing.

== Target release TBD

// Source: KOB-53962 from PR #561
// https://github.com/kobiton/docs/pull/561

=== Apps and app storage

- Fixed `GET /v2/apps/{appId}` omitting `latest_version` when the request sets `includeAllAppVersions=true`. The response now returns `latest_version` alongside `versions`, with the same value the endpoint returns when `includeAllAppVersions` is omitted or set to `false`.
- Fixed the `getApp` MCP tool returning `latest_version` as `null` when called with `includeAllVersions=true`.
- `latest_version` skips versions that are still importing or parsing. An app with no other versions returns no `latest_version`.

// Source: KOB-54591 from PR #569
// https://github.com/kobiton/docs/pull/569

- The MCP `listDevices` tool now documents how its `deviceName` filter matches. The filter matches case-insensitively against the full device name, and `*` wildcards select partial matches: `*galaxy*` returns every Galaxy device, `Galaxy*` returns names starting with Galaxy, and a bare `Galaxy` matches only a device whose full name is exactly `Galaxy`. Previously the tool description did not state this, so a partial name returned an empty device list with no explanation.
- `listDevices` now returns an `applied_filters` object when every device list in the response is empty. This distinguishes a filter that matched no devices from an organization that has no devices to list.
- The `listDevices` description now states that devices returned with `is_cloud: true` have restricted shell-command access in CLI sessions, and devices returned with `is_cloud: false` are unrestricted.

// Source: KOB-54477 from PR #556
// https://github.com/kobiton/docs/pull/556

=== Kobiton CLI reports refused file pushes

`kobiton file push` now reports a refused push and exits with a non-zero exit code. Previously a refused push printed `Pushed N bytes` and exited `0`, so a pipeline recorded a transfer that never reached the device.

A push is refused when the device does not permit writing to the destination path. Public cloud and trial devices run a restricted session that limits writes to the permitted directories.

This is a behavior change that affects automation workflows relying on CLI exit codes.

*Highlights*

- `file push` exits non-zero when the device refuses the destination path
- The CLI reports the reason for the refusal instead of a success message
- Successful pushes are unaffected
- CI/CD pipelines that check exit codes now detect refused pushes

*Availability*

Requires the latest Kobiton CLI. Download it from the Downloads icon in the left navigation of Kobiton Portal. Earlier CLI versions continue to report a refused push as successful.


== Editor review candidates

// These PRs may contain release-note-worthy changes.
// They did not include release-note partial files.
// Human review required before inclusion.

=== docs(KOB-54362): release-note draft

// PR: https://github.com/kobiton/docs/pull/560
// Reason: IT-Kobiton docs PR without a release-note partial. Changed customer-facing docs surface. Human review recommended.
// Signals:
// - author: IT-Kobiton
// - changed_path: docs/modules/

// Changed files:
// - docs/modules/ai/pages/mcp/ai-plugin-mcp-ref.adoc

=== Manual update from Device Management

// PR: https://github.com/kobiton/docs/pull/557
// Reason: IT-Kobiton docs PR without a release-note partial. Changed customer-facing docs surface. Human review recommended.
// Signals:
// - author: IT-Kobiton
// - changed_path: docs/modules/

// Changed files:
// - docs/modules/device-lab-management/pages/deviceConnect/remote-update-deviceconnect.adoc

=== docs(KOB-54705): release-note draft

// PR: https://github.com/kobiton/docs/pull/567
// Reason: IT-Kobiton docs PR without a release-note partial. Changed customer-facing docs surface. Human review recommended.
// Signals:
// - author: IT-Kobiton
// - changed_path: docs/modules/

// Changed files:
// - docs/modules/manual-testing/pages/adb-commands.adoc

=== Update Test Cases and add Start Mixed Session

// PR: https://github.com/kobiton/docs/pull/549
// Reason: IT-Kobiton docs PR without a release-note partial. Changed customer-facing docs surface. Human review recommended.
// Signals:
// - author: IT-Kobiton
// - changed_path: docs/modules/

// Changed files:
// - docs/modules/automation-testing/pages/start-a-mixed-session.adoc
// - docs/modules/test-management/pages/test-cases.adoc

=== Check which app version is installed

// PR: https://github.com/kobiton/docs/pull/548
// Reason: IT-Kobiton docs PR without a release-note partial. Changed customer-facing docs surface. Human review recommended.
// Signals:
// - author: IT-Kobiton
// - changed_path: docs/modules/

// Changed files:
// - docs/modules/devices/pages/install-an-app.adoc
