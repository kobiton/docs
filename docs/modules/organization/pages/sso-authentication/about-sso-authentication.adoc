= Sign in with Enterprise SSO
:navtitle: Sign in with Enterprise SSO

Enterprise SSO lets users of participating organizations sign in to Kobiton Portal through their corporate identity provider (IdP). After the first successful sign-in, your email address is recognized on subsequent visits and the sign-in flow skips directly to the Enterprise SSO step.

== Prerequisites

* Your organization uses Enterprise SSO with Kobiton.
* Your organization administrator has provisioned your account.

== Sign in for the first time

. Open the https://portal.kobiton.com/login[Kobiton Portal sign-in page].
. Enter your corporate email address and select *Continue*.
. Select *Continue with Enterprise SSO*.
. Complete sign-in on your organization's IdP screen.

// TUNG — Flow 2 reviewer questions
//
// Second Enterprise SSO scenario: user was NOT provisioned in Kobiton
// Portal by an admin. They were added only to their IdP group that
// grants Kobiton access, so Kobiton has no email record for them yet.
//
// UI flow observed on portal.kobiton.com/login (2026-09-22):
//   1. On the sign-in page, below the email/username field and the
//      "Don't have an account?" text, select "Join your company with SSO"
//      (clickable link).
//   2. On the "Log In To Your Account" screen, enter the company name
//      in the text box.
//   3. Select "Continue with SSO".
//   4. Complete sign-in on the org's IdP screen.
//
// Open questions before we can draft this section:
//   - What value should a user type into the "company name" text box?
//     Hypothesis: the Kobiton "Domain name" value referenced in
//     use-google-workspace.adoc (mapped to Google's Start URL field).
//     Tiffany tried her org's Domain name and hit a Microsoft "tenant
//     ID not found" error, so the value or format may be more nuanced.
//   - Where does an admin see this value in the SSO Settings UI so
//     they can communicate it to end users? get-kobiton-parameters.adoc
//     does not list it.
//   - Does Flow 2 require SSO enforcement to be turned on, or does it
//     also work when SSO is configured but not enforced?
//   - After the first successful Flow 2 sign-in, does Kobiton auto-
//     create the user (per the JIT behavior in the 4.13 release notes)
//     and route subsequent sign-ins through Flow 1?
//
// Once answered, the "Sign in for the first time" heading above will
// be restructured to distinguish the two scenarios.

== Related guides

* xref:organization:sign-in-to-portal.adoc[]
