= Manage users
:navtitle: Manage users

Learn how to manage your users so you can assign a role, add a user to a team, view user history, and more. xref:organization:users/search-for-a-user.adoc[Search for a user] to get started.

[#_enable_single_sign_on_sso_authentication]
== Enable Single-Sign On (SSO) authentication

After you xref:organization:sso-authentication/about-sso-authentication.adoc[configure SSO authentication for your organization], select *Details*, then *Enable SSO*.

image:organization:enable-sso-closeup.png[width=1000,alt="Select Details, then Enable SSO"]

When you're finished, select *Save*.

image:organization:click-save-enable-sso-context.png[width=1000,alt="Click Save after enabling SSO"]

== Disable the user

In *Details*, select *Disable* to deactivate the user but keep them in your system.

image:organization:disable-user-closeup.png[width=1000,alt="Select Disable to deactivate the user"]

When you're finished, select *Save*.

image:organization:disable-popup-closeup.png[width=500,alt="A closeup to the popup confirming disabling the user"]

== Remove the user

In *Details*, select *Remove* to completely remove the user from your system.

image:organization:remove-users-manage-users-closeup.png[width=1000,alt="A closeup to remove the user"]

When you're finished, select *Save*.

image:organization:remove-confirmation-popup-closeup.png[width=500,alt="A closeup to removing users confirmation popup"]

== Assign a role

In *Roles*, use the search bar to find a role you xref:organization:roles/create-a-role.adoc[previously created].

image:organization:search-for-roles-closeup.png[width=1000,alt="Open Roles tab in Users and search for a role"]

Select the *plus* icon next to the role.

image:organization:add-role-to-user-closeup.png[width=1000,alt="Add a role to the user"]

When you're finished, select *Save*.

image:organization:click-save-add-role-context.png[width=1000,alt="Click Save after adding the role to the user"]

== Remove a role

In *Roles*, select the *minus* icon next to the role you want to remove.

image:organization:remove-role-closeup.png[width=1000,alt="Remove a role from the user"]

When you're finished, select *Save*.

image:organization:click-save-remove-role-context.png[width=1000,alt="Click save after removing a role from the user"]

== Add to a team

In *Teams*, use the search bar to find a team you previously created.

image:organization:teams-tab-context.png[width=1000,alt="In Teams, use the search bar to find a team you previously created."]

Select the *plus* icon next to the team.

image:organization:team-click-plus-icon-closeup.png[width=1000,alt="Select the plus icon next to the team to assign the team for the user"]

When you're finished, select *Save*.

image:organization:click-save-add-teams-context.png[width=1000,alt="Click Save after adding a team to the user"]

== Remove from a team

In *Teams*, select the *minus* icon next to the team you want to remove the user from.

image:organization:remove-from-a-team-closeup.png[width=1000,alt=" select the minus icon next to the team you want to remove the user from"]

When you're finished, select *Save*.

image:organization:click-save-remove-teams-context.png[width=1000,alt="Click Save after removing a team"]

== View user history

In *User History*, you can view the xref:organization:users/user-history-report.adoc[user's history report].

image:$NEW-IMAGE$[width=,alt=""]
