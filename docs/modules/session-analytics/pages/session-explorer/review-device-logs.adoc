= Review device logs
:navtitle: Review device logs

Review device logs in the Session Explorer lets you read, filter, and copy device logs.

image:session-analytics:review-device-logs-context.png[width=1000,alt="A context to Review Device Logs feature in Session Explorer"]

== Filters

Filter logs to only display *Notice*, *Info*, *Warning*, or *Error* messages. By default, the filter is set to *All*.

image:session-analytics:review-device-logs-filter-closeup.png[width=500,alt="A closeup to the filter of Review Device Log feature"]

== Search bar

Filter the logs by specific criteria.

image:session-analytics:review-device-logs-search-closeup.png[width=500,alt="A closeup to the filter of Review Device Log feature"]

== Copy logs

Copy the current logs to your clipboard.

image:session-analytics:review-device-log-copied-context.png[width=1000,alt="A current log is copied in Review Device Log"]
